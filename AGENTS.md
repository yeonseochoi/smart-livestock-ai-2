# AGENTS.md

## 응답 원칙

- 한국어와 UTF-8을 사용하고 결론부터 간결하게 말한다.
- 담백한 `~임/~함` 체로 쓰고 미사여구·홍보성 표현을 피한다.
- 사실·가설·미확인 사항을 구분하고 불확실성을 숨기지 않는다.
- 주요 주장에는 출처와 확인일을 남긴다.
- 실제 통용되는 용어를 쓰고 명칭을 임의로 만들지 않는다. 예: "규칙 엔진" X / rule-base O.

## 문서 참조

- 관련 문서만 선택해서 읽고 프로젝트 문서를 일괄 로드하지 않는다.
- 프로젝트 목적·제품 구성·현재 성능·한계·실행 방법은 [PROJECT_CONTEXT.md](PROJECT_CONTEXT.md)를 확인한다.
- 데이터 범위·Event 정의·검증 프로토콜·산출물 규약은 [PROJECT_CONTEXT.md](PROJECT_CONTEXT.md)의 "데이터와 검증" 절을 확인한다.
- Track A(역추적 엔진)와 Track B(성능보존형 통합) 사이 인터페이스·파일 소유권·누수 규칙은 [docs/contracts/source_backtrack_contract.md](docs/contracts/source_backtrack_contract.md)를 확인한다.
- 사용자용 실행 안내와 공개 성능 수치는 [README.md](README.md)를 확인한다.
- 1차 멘토링 지적 사항은 `1차 멘토링 피드백.txt`를 확인한다.

## 작업 기준

- 자료 우선순위는 `실제 원본 파일·코드값 → 산출물 metrics.json → PROJECT_CONTEXT.md → README.md·메모` 순이다.
- 원본과 문서가 다르면 실제 파일을 기준으로 차이를 기록한다.
- 아이디어에 데이터를 끼워 맞추지 않고 데이터 존재·접근성·실제 컬럼을 먼저 확인한다.
- 민원은 시민 인지와 신고 행동의 결과이므로 악취 농도나 배출원 판정으로 표현하지 않는다. 시설을 원인으로 단정하지 않고 `후보`, `적합도`, `가능성`으로 쓴다.
- 상관관계를 인과관계로 표현하지 않는다.

## 확장 검정

- 새 데이터·특징·모델은 `멘토 지적 부합성`, `데이터 확보·관측 가능성`, `동일 분할·동일 지표에서의 성능 유지`, `현장 적용 가능성`으로 검정한다.
- 실제 컬럼으로 계산 가능한 신호와 행정 의사결정 산출물(브리핑·점검 지시서·사후 보고서)을 연결한다.
- 후보를 `채택`, `조건부 채택`, `보류`, `기각`으로 판정하고 근거와 미확인 사항을 기록한다.
- 핵심 데이터 부재, 없는 속성 추정, 누수 가능성, 적용 사용자 불명확 중 하나라도 해당하면 보류하거나 기각한다.
- 이미 실패한 시도(커밋 `ab93076`: Event 당시 풍향 + 정적 downwind 특징 29개, 테스트 PR-AUC −0.023)를 같은 방식으로 반복하지 않는다.

## 데이터 작업

- 원본 데이터(`data/*.xlsx`, `data/*.csv`)는 수정하거나 덮어쓰지 않는다.
- 정제본은 `outputs/` 아래 별도 저장하고 중요한 변환과 행 수 변화를 기록한다.
- 데이터에 없는 열·속성·시간·공간 단위를 추정해 생성하지 않는다.
- 결합 키는 `event_hour`다. `event_id`는 데이터가 바뀌면 번호가 밀리므로 키로 쓰지 않는다.
- 격자 원점은 민원 위·경도 중앙값이라 데이터가 바뀌면 이동한다. 격자 인덱스만 저장하지 않고 중심 위·경도를 함께 기록한다.
- Event별 특징은 `event_hour + 30분`까지의 정보만 쓴다. 이후 30분 민원과 미래 민원은 어떤 형태로도 참조하지 않는다.
- 결측은 0이 아니라 NaN으로 남긴다.
- 지오코딩 API 결과(좌표 포함 발생원 테이블)는 커밋하지 않는다.
- 결합 전 분석 단위, 키 유일성, 교집합, 시간·공간 단위를 검정한다.

## 성능 비교

- 비교는 `compare_operational_grid_sizes.py`의 공통 Event 160개, 시간순 112/48 분할, PR-AUC·Top-K Recall·Recall@3·Event Hit@3로만 한다.
- 모델·특징 선택은 학습 구간 마지막 20% Event 내부검증으로만 하고 최종 테스트는 1회 사용한다.
- 기준선 M0(`xgb_d3`, Event Hit@3 0.851, PR-AUC 0.486)을 고정하고 소거 실험으로 비교한다. 내부검증에서 개선이 없으면 M0을 유지한다.
- 추가 특징은 물리적으로 설명 가능한 3~5개로 제한한다.

## 병렬 작업 규칙

- Track A(Codex)는 `feat/source-backtrack`, Track B(Claude)는 `feat/integration-ablation`에서 작업하고 main에 직접 커밋하지 않는다.
- 파일 소유권은 계약 문서를 따른다. 상대 트랙 소유 파일은 수정하지 않는다.
- 산출물은 `python tools/validate_backtrack_output.py`를 통과해야 한다. 실패하면 산출물을 고치고 검증기는 고치지 않는다.
- 막히면(키 없음, 계약 문서 없음, 데이터 불일치) 추측으로 채우지 않고 멈추고 묻는다.

## 구현 착수 조건

- 계약 문서, 검증기, 기준 후보 격자표, 브랜치가 준비되기 전에는 Track 작업을 시작하지 않는다.
- 새 데이터(예: 공장·하수처리시설 위치)를 추가할 때는 `sources.csv`의 `source_type`으로 구분하고 계약 스키마는 바꾸지 않는다.
