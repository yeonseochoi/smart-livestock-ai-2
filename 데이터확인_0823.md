# 외부 데이터셋 검토 기록 (2026-08-23)

## 왜 외부 데이터를 찾았나

현재 예측 엔진은 민원 데이터만 사용한다. 15개 입력 피처 중 `prior`(과거 Event에서 해당
격자가 실제 정답이었던 비율) 하나가 인구 밀도, 배출원 근접도, 신고 성향을 한꺼번에
흡수하고 있다. 이 셋을 분리해 직접 넣으면 예측이 개선될 수 있다고 보고 후보 데이터를 찾았다.

## 판정 기준

예측 단위는 `(Event 1개, 1km 후보 격자 1개)` = 1행이다. 1km 격자 기준 한 Event당 후보
격자가 평균 34개이고 모델은 이 34개의 **순위**를 매긴다.

따라서 피처가 되려면 **같은 Event 안에서 격자마다 값이 달라야** 한다. 익산시 전체에
숫자 하나가 붙는 데이터는 34개 격자에 같은 값이 들어가므로 순위 결정에 기여하지 않는다.

시간 해상도도 30분 단위 예측에 맞아야 한다.

## 검토 결과 요약

| 데이터셋 | 제공기관 | 공간 해상도 | 시간 해상도 | Event 내 변동 | 판정 |
|---|---|---|---|---|---|
| 축산분뇨 처리시설업체 | 농촌진흥청 농사로 | 업체 소재지 | 평가년도 | — | 기각 |
| 축산분뇨 농가활용프로그램 | 농촌진흥청 농사로 | 없음 | 없음 | — | 기각 |
| 축산환경자가진단 | 농촌진흥청 농사로 | 없음 | 없음 | — | 기각 |
| 경상남도 축산농가 | 경상남도 | — | — | — | 호출 실패 |
| 돼지분뇨 배출량 | 한국환경공단 | 시군구 | 월 | **0%** | 기각 |
| 돼지액비 배출량 | 한국환경공단 | 시군구 | 월 | **0%** | 기각 |
| **통계지리정보(SGIS)** | 통계청 | **읍면동** | 연 | **100%** | **실험 후 기각** |

7개 중 판정 기준을 통과한 것은 SGIS 하나였고, 실제로 학습·평가까지 돌린 결과 성능이
개선되지 않아 최종 기각했다.

---

## 1. 농사로 API 3종

세 데이터셋 모두 농촌진흥청 농사로 OpenAPI로 제공되며, 공공데이터포털(data.go.kr)
인증키로는 호출되지 않는다. 농사로에서 별도 인증키를 발급받아야 한다.

```
GET http://api.nongsaro.go.kr/service/processFcltyEntrps/processFcltyEntrpsList?apiKey=TEST
→ resultCode 11 "인증키가 등록되지 않았습니다. 농사로에 접속하여 발급받으십시오."
```

키를 받지 못해 실데이터는 확인하지 못했고, 아래는 API 매뉴얼의 응답 스키마로 판정했다.
매뉴얼은 AJAX 방식만 안내하지만 일반 REST GET으로도 XML이 반환되므로, 키를 받으면
서버 측에서 수집할 수 있다.

### 1.1 축산분뇨 처리시설업체 (`processFcltyEntrps`)

응답 필드에 주소(`entrpsAdres`)가 있어 배출원 좌표로 쓸 수 있는지 확인했다.

**기각 사유**: 이 데이터는 분뇨 처리시설을 시공·운영하는 **업체 목록**이다. 주소는 업체
본사 소재지이지 실제 처리시설이나 축산농장의 위치가 아니다. 전국 대상이므로 익산 소재
업체가 있어도 익산의 악취 배출원과는 무관하다.

다만 `processMsrcNm`(처리공법), `fcltyTyNm`(시설유형), `evlResultCn`(평가결과)은 공법별
특성을 설명하는 참조 자료로는 쓸 수 있다.

### 1.2 축산분뇨 농가활용프로그램 (`farmAppTech`)

응답 필드: 게시물 제목, 작성자, 등록일시, 조회수, 첨부파일 URL.

**기각 사유**: 데이터셋이 아니라 자료실 게시판이다. 수치, 좌표, 농가 식별자가 없다.

### 1.3 축산환경자가진단 (`stkrsEnvrnJdgmn`)

응답 필드: 심사명, 축종 코드, 심사 문항 목록, 문항 보기 목록.

**기각 사유**: 제공되는 것은 진단 **설문지 자체**(문항과 보기)이고, 농가별 진단 결과나
점수는 포함되지 않는다. 농가 식별자와 좌표도 없다.

---

## 2. 한국환경공단 돼지분뇨·액비 배출량

두 API 모두 정상 호출됐다. 익산시 실측값을 확보했다.

**응답 구조** — 1행 = 관할관청(시군구) × 배출년월 × 축분종류

| 필드 | 설명 |
|---|---|
| `cmptncGovofcNm` / `cmptncGovofcLaeCd` | 관할관청명 / 지자체 코드 (익산 = `1209`) |
| `emsnYm` / `ssnNm` | 배출 년월 / 계절 |
| `emsnQnt`, `lqdftEmsnQnt` | 분뇨 배출량, 액비 배출량 |
| `emsnCrrgVhclCnt` | 배출 운반 차량수 |

**익산시 돼지분뇨 배출량 (연도별)**

| 2019 | 2020 | 2021 | 2022 | 2023 |
|---|---|---|---|---|
| 235,591 | 237,718 | 218,091 | 222,271 | 227,540 |

익산시 돼지액비 배출량 전체기간 누적: 404,502

**기각 사유**: 공간 단위가 시군구다. 익산시 전체에 월 단위 숫자 하나가 붙으므로 한 Event의
후보 격자 34개가 모두 같은 값을 갖는다. 순위 결정 기여도가 정의상 0이다. 시간 단위도
월이라 30분 예측과 1,440배 차이가 난다.

농가나 시설 단위 좌표는 두 API 어디에도 없다.

**다만 기록해 둘 것**: `emsnCrrgVhclCnt`(운반 차량수)는 액비 살포량의 대리변수가 될 수
있다. 액비 살포는 축산 악취의 직접적 원인이다. 격자 순위에는 쓸 수 없지만, "이번 달은
민원이 많이 발생할 달인가"를 다루는 Event 발생 예측으로 확장할 경우 후보가 된다.

---

## 3. 경상남도 축산농가

```
GET https://apis.data.go.kr/6480000/gyeongnamlivestockfarmsituation
→ HTTP 400, NO_OPENAPI_SERVICE_ERROR (returnReasonCode 12, "서비스가 없거나 폐기됨")
```

오퍼레이션명·대소문자·프로토콜을 바꿔 8가지 경로를 시도했으나 모두 같은 오류였다.
같은 인증키로 §2의 두 API는 정상 통과했으므로 키 문제는 아니다. 서비스 폐기 또는 신규
승인 반영 지연으로 보인다.

**다만 호출에 성공하더라도 대상 지역이 경상남도이므로 익산시 프로젝트에는 쓸 수 없다.**
전라북도·익산시에 동일한 축산농가 API가 있는지 확인하는 참고 사례로서만 의미가 있다.

---

## 4. 통계청 통계지리정보(SGIS) — 실험 후 기각

### 4.1 확보한 데이터

익산시 읍면동 29개 단위로 다음을 수집했다.

| 항목 | 엔드포인트 | 기준연도 |
|---|---|---|
| 인구 | `stats/searchpopulation.json` | 2023 |
| 가구 | `stats/household.json` | 2023 |
| 주택 | `stats/house.json` | 2023 |
| 농가 | `stats/farmhousehold.json` | 2020 |
| 사업체 | `stats/company.json` | 2022 |
| 행정동 경계 폴리곤 | `boundary/hadmarea.geojson` | 2023 |

인증에는 서비스 ID(`consumer_key`)와 보안 Key(`consumer_secret`) 두 개가 모두 필요하다.

### 4.2 왜 이것만 조건을 통과했나

SGIS OpenAPI는 격자 통계를 제공하지 않고 행정구역 단위로만 제공한다. 읍면동 29개를
1km 격자 약 500개에 대응시키면 격자 17개당 1개 값이라 변별력이 부족할 것으로 예상했으나,
실제로 측정하니 그렇지 않았다.

```
Event당 후보 격자 평균 34.3개
→ 걸치는 행정동 수: 평균 11.14개 (중앙값 12, 최대 18)
→ 행정동 1개에만 속한 Event 비율: 0.0%
→ Event 내 변동률: 100%
```

익산 행정동은 크기 편차가 커서(시내 동은 작고 면은 큼), 후보 격자 3개당 행정동 1개꼴로
갈린다. 피처로서의 기술적 자격은 충족했다.

경계 폴리곤으로 공간조인하니 민원 14,994건이 100% 매칭됐다. 민원 데이터의 `region`
필드는 법정동이고 SGIS는 행정동이라 이름으로는 조인되지 않으므로, 좌표 기반 조인이 필요하다.

### 4.3 어떤 피처를 쓰려 했나

격자 단위 상관을 먼저 확인해 후보를 하나로 좁혔다.

| | target과의 상관 |
|---|---|
| `prior` (기존 피처) | **0.528** |
| 주택 수 | 0.132 |
| 인구 | 0.125 |
| 농가 수 | **−0.035** |

- **주택 수를 선택**했다. SGIS 항목 중 target 상관이 가장 높고, 악취 민원이 야간에
  집중되는 특성상 주민등록 인구보다 상주 노출을 더 잘 나타낸다.
- 인구는 주택과 상관이 0.969로 사실상 같은 정보라 함께 넣지 않았다.
- **농가 수는 상관이 음수라 제외**했다. 농림어업총조사 기준 전체 농가로, 익산은 쌀
  주산지이므로 대부분 경종 농가다. 축산농가만 분리된 값이 아니다.

`prior`와 주택 수의 상관은 0.258로 중복도가 낮아, 정보 자체는 새로운 것으로 판단했다.

### 4.4 실험 설계

`evaluate_sgis_feature.py`로 기존 15개 피처와 `house_cnt`를 더한 16개 피처를 같은 분할·
같은 모델 선택 규칙으로 비교했다.

- 1km 격자, Event 시간순 70/30 분할 (학습 115 / 테스트 50)
- 학습 기간 내부 검증(마지막 20%)으로 5개 후보 모델 중 선택, 미래 테스트는 한 번만 사용
- 두 구성이 **같은 Event를 함께 재추출**하는 짝지은 부트스트랩 4,000회 (시드 20260823)

테스트 Event가 50개뿐이라 지표 차이가 쉽게 흔들린다. 단일 수치 비교로는 판정할 수 없어
신뢰구간과 차이 분포를 함께 계산했다.

### 4.5 결과

```
[baseline]   15개 피처        선택 xgb_d3   PR-AUC 0.4955   Hit@3 0.8367
[with_sgis]  15 + house_cnt   선택 xgb_d3   PR-AUC 0.4866   Hit@3 0.8163
```

| 지표 | baseline | +주택 수 | 차이 | 95% CI | 개선 확률 |
|---|---|---|---|---|---|
| Event Hit@3 | 0.8367 | 0.8163 | −0.0204 | [−0.0612, 0.0000] | **0.00** |
| Recall@3 | 0.4256 | 0.4194 | −0.0061 | [−0.0327, +0.0163] | 0.29 |

두 구성 모두 내부 검증에서 `xgb_d3`가 선택됐다. 즉 피처를 하나 더 준 쪽이 같은 모델로
같은 분할을 학습하고도 테스트 성능이 낮았다.

### 4.6 왜 안 되는가

**Hit@3 개선 확률이 0.00이다.** 4,000회 재추출에서 단 한 번도 앞서지 못했다. 주택 수를
넣은 모델이 맞힌 Event가 기존 모델이 맞힌 Event의 부분집합이라는 뜻으로, Event 하나를
잃고 새로 얻은 것은 없다. Recall@3은 신뢰구간이 0을 걸쳐 노이즈로 판단한다.

원인은 `prior`가 이미 같은 정보를 더 정확하게 담고 있기 때문으로 보인다. `prior`는 해당
격자가 실제로 민원 대상이 된 빈도를 직접 측정한 값이고, 주택 수는 그것을 읍면동 해상도로
근사한 값이다. 직접 측정값이 있는 상태에서 거친 근사를 더하면 잡음만 늘어난다.

**보완이 필요할 것으로 예상했던 cold-start 문제도 실재하지 않았다.**

```
prior = 0 인 격자:  학습 27.4% (1,104행) / 테스트 29.1% (435행)
그중 실제 양성:     0개 (양쪽 모두)
```

`prior = 0`인 격자는 실제로 정답이 된 적이 한 번도 없다. 학습 기간에서 계산한 `prior`로
미래 테스트를 평가해도 마찬가지다. 즉 `prior = 0`은 "학습 데이터가 없어 모르는 상태"가
아니라 "실제로 위험이 없음"을 정확히 표시하고 있었고, 인구·주택으로 채울 공백이 없었다.

---

## 5. 결론

검토한 7개 데이터셋 모두 현재 예측 엔진의 격자 피처로는 채택하지 않는다.

- 농사로 3종: 수치·좌표 데이터가 아니거나 배출원 위치가 아님
- 환경공단 2종: 시군구 × 월 해상도로 Event 내 변동이 0
- 경상남도: 호출 실패 및 대상 지역 불일치
- SGIS: 기술적 자격은 충족했으나 실험 결과 성능이 개선되지 않음

`PROJECT_CONTEXT.md`의 "센서·기상·배출원 정보가 모델 성능을 개선한다는 근거는 아직
확보되지 않았다"는 서술에 SGIS 인구·주택 통계도 추가할 수 있다. 이번 검토는 그 근거를
수치로 남긴 것이다.

## 6. 부수적으로 확인된 사실

모델 개선과는 별개로, 아래 두 가지는 프로젝트의 한계 서술을 뒷받침하는 근거가 된다.

### 6.1 민원은 배출원이 아니라 인구를 따라간다

행정동 29개 단위 상관 (민원 14,994건 좌표 기반 조인)

| | 민원 건수와의 상관 |
|---|---|
| 주택 수 | 0.730 |
| 인구 | 0.693 |
| 사업체 수 | 0.545 |
| **농가 수** | **0.110** |

`PROJECT_CONTEXT.md`는 "민원은 시민 인지와 신고 행동의 영향을 받으므로 실제 악취
영향권과 동일하지 않다"고 서술하고 있으나 근거가 제시되어 있지 않다. 위 수치가 그 근거다.

다만 인구로 정규화하면 양상이 달라진다. 인구 천명당 민원 건수 상위는 왕궁면 284.0,
낭산면 209.3, 팔봉동 118.5, 춘포면 111.7 순으로, 인구가 적은 농촌 읍면이 앞선다.
민원 절대 건수 1위인 삼성동(2,824건)은 인구 30,995명의 결과이며 천명당으로는 91.1이다.

즉 인구가 주효과이고, 인구 대비 초과 민원은 별도 요인으로 설명해야 한다.

### 6.2 무작위 기준선

현재 저장소에는 예측 성능을 비교할 기준선이 없다. 이번 실험에서 계산한 값은 다음과 같다.

```
1km 격자 무작위 선택 Event Hit@3 기대값 = 0.3153
현재 모델 = 0.8367  →  2.65배
```

---

## 재현 방법

SGIS 실험 코드는 기각된 피처이므로 저장소에 포함하지 않았다. 재현이 필요하면 아래
설정으로 다시 구성한다.

- 데이터: SGIS OpenAPI3 `stats/house.json`, `boundary/hadmarea.geojson` (익산 `adm_cd=35030`,
  `low_search=1`, 2023년)
- 격자 대응: 격자 중심 좌표를 WGS84 → EPSG:5179 변환 후 행정동 폴리곤에 point-in-polygon.
  시 경계 밖 후보 격자는 최근접 행정동 값을 사용 (0을 넣으면 피처가 경계 여부를 대신
  학습한다)
- 평가: 1km 격자, Event 시간순 70/30, 내부검증 모델 선택, 짝지은 부트스트랩 4,000회,
  시드 20260823
