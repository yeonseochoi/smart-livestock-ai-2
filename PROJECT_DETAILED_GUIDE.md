# 익산시 악취 민원 선제 대응 프로젝트 상세 설명서

## 1. 프로젝트 한눈에 보기

이 프로젝트는 익산시 악취 민원 이력을 이용해 **현재 이벤트의 최초 30분을 관찰한 뒤, 다음 30분 동안 추가 민원이 접수될 가능성이 높은 1km 격자 3곳을 우선순위로 제시**하는 경진대회용 MVP다. 예측 결과를 지도에서 보여 주고, 지자체 담당자가 바로 검토할 수 있는 상황 브리핑·현장 점검 지시서·사후 결과보고서도 생성한다.

핵심 목적은 악취 발생원이나 농가를 자동 판정하는 것이 아니라, 제한된 순찰·측정 자원을 어디부터 투입할지 결정하는 것이다. 따라서 모델 출력은 절대적인 발생 확률이 아니라 **동일 이벤트 안에서 비교하는 상대 위험 순위**로 해석해야 한다.

전체 흐름은 다음과 같다.

```text
악취 민원 Excel
  → 정제·익산시 필터링
  → 시공간 이벤트 구성
  → 최초 30분/향후 30분 분리
  → 주변 후보 격자와 특징 생성
  → 시간순 학습·검증 및 순위 모델 적용
  → 1km Top 3 위험 권역
  → 지도 데모 + 행정 대응 문서 3종
  → 담당자 현장 결과 입력 및 사후보고서 저장
```

## 2. 해결하려는 문제와 결과의 의미

기존 민원 조회 시스템이 이미 들어온 신고와 관측 상황을 보여 준다면, 이 프로젝트는 그 정보에서 한 단계 더 나아가 가까운 미래의 신고 확산 위치를 예측한다.

- 관찰 구간: 이벤트 시작 시각부터 30분
- 예측 구간: 관찰 구간 직후 30분
- 공간 단위: 1km × 1km 격자
- 운영 출력: 이벤트별 위험 상위 3개 격자
- 사용자: 악취 민원 대응 담당자, 순찰·측정 인력
- 활용 예: 차량 이동 순서, 현장 확인 순서, 추가 민원 감시 우선순위 결정

모델이 직접 말할 수 있는 것은 “과거 민원 양상과 현재 초기 신고 분포를 볼 때 어느 격자에 추가 신고가 들어올 상대적 가능성이 높은가”까지다. 다음 사항은 판정하지 않는다.

- 실제 악취 농도 또는 악취 발생 여부
- 특정 축산시설·사업장이 원인인지 여부
- 법적 책임이나 행정처분 대상
- 민원 위험 점수를 보정된 확률로 해석한 수치
- 대응 조치의 효과

## 3. 데이터

### 3.1 원본 데이터

원본은 `data/익산시 악취 민원 데이터_20190528-20260818.xlsx`다. 프로젝트 문맥에 기록된 데이터 규모는 다음과 같다.

|항목|값|
|---|---:|
|전체 행|16,113건|
|익산시 민원|14,994건|
|실제 시각 범위|2019-05-28 23:23 ~ 2026-08-17 22:54|
|공통 평가 이벤트|160개|
|시간순 학습 이벤트|112개|
|시간순 테스트 이벤트|48개|

`analyze_spatiotemporal_complaints.py`는 열 이름의 여러 표기를 별칭으로 인식하고, 시간·위경도·강도 등의 필드를 표준화한다. 기본 분석 대상은 익산시이며 완주군 삼례읍 인접 자료를 별도로 살펴볼 수 있는 로직도 포함한다. 동일 위치 반복, 거리·시간 근접성, 시간대별 집계, 대규모 이벤트 및 행정경계 인접 이벤트 분석 기능도 제공한다.

### 3.2 이벤트 정의

모델은 민원 한 건씩을 독립 샘플로 취급하지 않고, 한 시간대에 함께 나타난 신고 묶음을 이벤트로 구성한다. 공용 이벤트 구성 로직은 `build_odor_ai_mvp.py`의 `build_bounded_events()`에 있다. 이벤트 시작 후 한 시간 안의 민원을 불러와 앞 30분을 입력으로, 뒤 30분을 정답으로 사용한다.

민감도 데이터 구성 시에는 다음 조건을 충족하는 이벤트만 사용한다.

- 입력 구간에 서로 다른 격자가 2개 이상 존재
- 미래 구간에 민원이 있는 격자가 1개 이상 존재

이 조건은 초기 상황만으로 공간 확산을 예측할 수 있고, 평가할 미래 정답도 있는 사례를 남기기 위한 것이다.

### 3.3 격자 좌표

위도·경도를 데이터 중앙값 기준의 평면 미터 좌표로 근사 변환한 뒤 격자 크기로 나누어 `grid_x`, `grid_y`를 만든다. 운영 단계의 식별자는 `G+X:+Y` 형태다. 이 격자는 행정동 경계가 아니라 모델 계산을 위한 고정 공간 단위다.

## 4. 예측 모델 파이프라인

### 4.1 후보 격자 생성

도시 전체의 모든 격자를 매번 평가하지 않는다. 최초 관찰 구간에서 민원이 발생한 격자를 중심으로 약 1.5km 범위의 주변 셀을 후보로 만든다. `sensitivity_early_prediction.py`는 격자 크기에 맞추어 후보 반경을 조절하고, `build_odor_ai_mvp.py`의 `candidate_cells()`와 `cell_features()`를 재사용한다.

각 후보 셀의 `target`은 향후 예측 구간에 실제 민원이 있으면 1, 없으면 0이다.

### 4.2 입력 특징

현재 운영 비교에서 사용하는 주요 특징은 다음 15개다.

|특징|의미|
|---|---|
|`initial_count`|초기 구간에서 후보 격자의 민원 수|
|`initial_intensity`|초기 민원 강도 요약|
|`min_distance`|초기 관측 격자까지의 최소 거리|
|`neighbor_count`|인접 영역의 민원 수|
|`radius3_count`|더 넓은 반경 안의 민원 수|
|`prior`|학습 이벤트에서 해당 격자에 미래 민원이 있었던 빈도|
|`hour_sin`, `hour_cos`|시간대의 순환형 표현|
|`centroid_distance`|초기 민원 중심점과 후보 격자 사이 거리|
|`nearest_intensity`|가장 가까운 초기 민원의 강도|
|`weighted_intensity`|거리를 반영한 강도|
|`observed_grid_count`|초기 구간의 활성 격자 수|
|`observed_report_count`|초기 구간의 전체 민원 수|
|`first15_count`|초기 전반부 15분의 민원 수|
|`growth`|초기 전·후반의 신고 증가 양상|

`optimize_early_prediction.py`에는 시간대·계절·주말·악취 유형·최근성·공간 평활 prior 등 고급 특징을 계산하는 함수도 있다. 다만 현재 운영 비교의 기본 `FEATURES` 목록은 위 15개이며, 기상 특징은 최종 점수에 포함하지 않는다.

### 4.3 후보 모델

프로젝트는 Extra Trees 계열, Random Forest 계열, XGBoost 이진 분류기, XGBoost 순위 학습 모델을 실험할 수 있다. 운영 격자 비교에서는 다음 다섯 후보를 내부 검증한다.

- `extra_leaf4`
- `xgb_d2`, `xgb_d3`
- `rank_d2`, `rank_d3`

`rank_d*`는 이벤트별 후보 격자를 하나의 그룹으로 묶고 `rank:pairwise` 목적함수로 학습한다. 운영 목표 자체가 확률 추정보다 이벤트 안의 Top 3 정렬이므로 순위 학습과 잘 맞는다. 분류 모델의 경우 0/1 예측값이 아니라 양성 확률을 순위 점수로 사용한다.

### 4.4 시간 누수 방지와 모델 선택

무작위 분할이 아니라 이벤트 시각순 70/30 분할을 사용한다. 과거 112개 이벤트로 학습하고 미래 48개 이벤트로 최종 평가한다. 학습 구간 안에서도 앞쪽 80%로 후보 모델을 학습하고 뒤쪽 20%로 내부 검증하여 모델을 선택한 후, 전체 학습 구간으로 다시 학습한다.

과거 발생 빈도인 `prior` 역시 학습 이벤트만으로 다시 계산한다. 테스트 이벤트의 미래 정보가 특징에 섞이지 않도록 하기 위한 조치다.

```text
전체 이벤트(시간순)
├─ 과거 70%: 학습 구간
│  ├─ 앞 80%: 후보 모델 학습
│  └─ 뒤 20%: 모델 선택용 내부 검증
└─ 미래 30%: 최종 테스트(모델 선택에 사용하지 않음)
```

## 5. 검증 결과

`outputs/operational_grid_comparison/metrics.json`은 1km·1.5km·2km를 동일한 160개 이벤트와 같은 시간 분할로 비교한 결과다. 모든 크기에서 내부 검증으로 `rank_d2`가 선택되었다.

|격자|PR-AUC|ROC-AUC|Top-K Recall|Recall@3|Event Hit@3|Top 3 점검면적|
|---:|---:|---:|---:|---:|---:|---:|
|1km|0.472|0.908|0.494|0.444|0.830|3km²|
|1.5km|0.603|0.874|0.612|0.648|0.935|6.75km²|
|2km|0.652|0.889|0.634|0.665|0.915|12km²|

더 큰 격자는 하나의 셀이 넓어서 적중 지표가 올라가기 쉽지만, Top 3만 점검해도 확인해야 할 면적이 크게 증가한다. 프로젝트가 1km를 운영 단위로 선택한 것은 정확도 수치만이 아니라 현장 점검 범위의 구체성과 실행 가능성을 함께 고려한 결정이다.

지표는 다음처럼 읽는다.

- **PR-AUC**: 양성 격자가 적은 불균형 상황에서 위험 순위가 실제 양성을 얼마나 잘 위에 올리는지 나타낸다.
- **ROC-AUC**: 임의의 양성·음성 격자 쌍에서 양성을 더 높게 평가할 가능성을 나타낸다.
- **Top-K Recall**: 이벤트별 양성 격자 수에 맞춘 상위 K 예측이 실제 양성을 얼마나 회수하는지 나타낸다.
- **Recall@3**: 고정된 상위 3개 격자에서 실제 양성 격자를 회수한 평균 비율이다.
- **Event Hit@3**: 평가 가능한 이벤트 중 Top 3에 실제 추가 민원 격자가 하나 이상 포함된 이벤트 비율이다. 1km의 0.830은 약 83.0%를 뜻한다.

`outputs/early_prediction_sensitivity/sensitivity_metrics.json`은 격자 250·500·750·1000m와 여러 입력/예측 시간을 폭넓게 비교한 별도 실험이다. 이 실험은 내부 검증에서 `g1000_i30_f30`과 `rank_d3`를 선택했고, 35개 테스트 이벤트에서 PR-AUC 0.469, ROC-AUC 0.893, Top-K Recall 0.446을 기록했다. 공통 이벤트를 강제한 운영 격자 비교와 평가 표본 및 선택 절차가 다르므로 두 파일의 수치를 직접 동일 실험처럼 비교하면 안 된다.

## 6. 기상 데이터의 위치

`fetch_kma_weather.py`는 기상청 API Hub에서 관측소 목록과 ASOS/AWS 자료를 수집하고, 이벤트별 기상 특징을 만든다. 주요 처리 내용은 다음과 같다.

- 이벤트 위치와 가까운 관측소 선택
- 월 단위 ASOS 수집과 이벤트 시간대 AWS 수집
- 풍향을 사인·코사인 성분으로 합성
- 풍속, 최대 풍속, 강수, 습도, 기온 집계
- 원본 응답 캐시 및 이벤트별 특징 CSV 저장

기상 자료는 현장 담당자의 상황 이해와 데모 표시에 활용되지만, 현재 최종 예측 점수에는 사용하지 않는다. 동일 시간 분할 실험에서 성능 개선이 작고 일관되지 않아, 검증되지 않은 개선을 주장하지 않기 위한 설계다.

## 7. 행정 대응 Agent

### 7.1 예측 엔진과의 경계

`administrative_agent/`는 원본 민원, 센서 또는 시설 정보를 직접 조회하지 않는다. 예측 엔진이 만든 표준 `ForecastResult`만 입력받으며 다음 제약을 코드로 강제한다.

- 격자 크기는 반드시 1km
- 위험 권역은 정확히 3개
- 순위는 1, 2, 3 순서
- 모든 문서는 `review_required=True`

이는 문서 생성 기능이 예측 결과를 임의로 바꾸거나 원인 시설을 추론하지 못하도록 역할을 분리한 것이다.

### 7.2 생성 문서

`build_response_package()`는 다음 세 문서를 만든다.

1. `complaint_briefing.md`: 이벤트 시각, Top 3 권역, 상대 위험도와 모델 한계를 담은 상황 브리핑
2. `field_inspection_order.md`: 상대 위험도에 따른 점검 순서와 현장 확인 항목
3. `followup_report_template.md`: 추가 민원·악취 감지·측정 결과·조치 내용을 기록할 사후 양식

구조화된 전체 결과는 `agent_output.json`으로도 저장되어 다른 서비스가 연동할 수 있다. 상대 위험도 80 이상은 “우선 점검”, 60 이상은 “순차 점검”, 그 미만은 “상황 관찰” 문구로 변환된다.

### 7.3 템플릿과 LLM 모드

API 키가 없거나 `LLM_PROVIDER=template`이면 코드에 정의된 안전 템플릿만 사용한다. Gemini 또는 OpenAI가 설정되어 있으면 먼저 안전 템플릿을 만든 후 LLM이 문장 표현을 다듬는다.

LLM 요청에는 다음 안전 조건이 들어간다.

- 수치·격자·순위를 변경하지 않음
- 새 사실을 만들지 않음
- 원인 시설과 실제 악취 발생을 단정하지 않음
- 담당자 검토 필요와 상대 우선순위라는 한계를 포함
- JSON Schema에 맞는 문서 3종만 반환

요청 시간 제한은 90초이며, 일시적 네트워크 오류나 429/5xx 응답은 한 번 재시도한다. 호출이 실패하면 데모 서버는 안전 템플릿으로 자동 대체하고 경고를 응답한다.

## 8. 웹 데모

`demo/index.html`은 지도 기반 운영 화면이고 `demo/demo-data.js`가 브라우저용 이벤트 데이터를 제공한다. `demo/build_demo_data.py`는 검증 결과, 원 민원 좌표, 이벤트별 기상을 조합하여 이 파일을 생성한다. 지도에는 초기 민원 위치, 후보 격자 점수, 실제 미래 민원 포함 여부, 기상 상황 등을 표시할 수 있다.

`demo/server.py`는 외부 프레임워크 없이 Python 표준 라이브러리의 `ThreadingHTTPServer`로 정적 파일과 API를 함께 제공한다.

|메서드·경로|기능|
|---|---|
|`GET /api/status`|LLM 설정 여부, 공급자, 현재 모드 확인|
|`POST /api/agent/generate`|선택 이벤트의 Top 3를 읽고 문서 3종 생성·저장|
|`POST /api/agent/followup`|담당자가 입력한 현장 결과로 완성 보고서 생성·저장|

서버는 보안을 위해 `127.0.0.1`에만 바인딩되며 기본 포트는 8765다. API 키는 브라우저로 전달하지 않고 서버 프로세스의 환경변수에서만 읽는다.

## 9. 디렉터리와 파일별 역할

```text
프로젝트 루트/
├─ data/                              원본 민원 데이터
├─ outputs/                           분석·모델·문서 산출물
│  ├─ operational_grid_comparison/   운영 격자 비교 지표와 테스트 예측
│  ├─ early_prediction_sensitivity/  민감도 실험 결과
│  ├─ weather_integration/            수집 기상 및 이벤트 특징
│  └─ administrative_agent/           브리핑·지시서·보고서·JSON
├─ administrative_agent/             행정 문서 도메인 모델과 생성 서비스
├─ demo/                              웹 UI, 로컬 API 서버, 데모 데이터 빌더
├─ tests/                             행정 Agent 단위 테스트
├─ analyze_spatiotemporal_complaints.py  탐색적 시공간 분석
├─ build_odor_ai_mvp.py               전처리·이벤트·특징·기초 실험 공용 코드
├─ sensitivity_early_prediction.py    격자/시간 조합 민감도 실험
├─ optimize_early_prediction.py       모델 생성·학습·평가 라이브러리
├─ compare_operational_grid_sizes.py  1/1.5/2km 공정 비교 및 운영 결과 생성
├─ fetch_kma_weather.py               기상청 데이터 수집·결합
├─ generate_agent_documents.py        예측 CSV를 행정 문서 입력으로 변환
├─ build_proposal_docx.py             경진대회 제안서 DOCX 생성
├─ run_demo.bat                       Gemini 사용 가능 데모 실행기
└─ run_demo_template.bat              API 호출 없는 템플릿 데모 실행기
```

주요 산출물은 다음과 같다.

|경로|내용|
|---|---|
|`outputs/operational_grid_comparison/metrics.json`|격자별 모델 선택 과정과 최종 테스트 지표|
|`outputs/operational_grid_comparison/test_predictions.csv`|이벤트·격자별 정답과 모델 점수|
|`outputs/early_prediction_sensitivity/sensitivity_metrics.json`|격자/입력/예측 시간 민감도 결과|
|`outputs/weather_integration/event_weather_features.csv`|이벤트별 기상 특징|
|`outputs/administrative_agent/agent_output.json`|예측과 문서가 합쳐진 연동용 결과|
|`스마트축산_AI_경진대회_제안서_초안.docx`|프로젝트 제안서 결과물|

## 10. 설치와 실행

### 10.1 요구 환경

- Windows 배치 실행 기준 Python 3.12
- `pandas`, `numpy`, `openpyxl`
- `scikit-learn`, `xgboost`
- `matplotlib`, `folium`
- `requests`

직접 설치하려면 다음 명령을 사용한다.

```powershell
python -m pip install -r requirements.txt
```

### 10.2 가장 쉬운 데모 실행

- `run_demo.bat`: `.venv`와 의존성을 확인한 뒤 Gemini 사용 가능 모드로 서버 실행
- `run_demo_template.bat`: `LLM_PROVIDER=template`을 설정해 외부 API 없이 실행

두 파일 모두 필요한 경우 Python 3.12 가상환경과 패키지를 준비하고 브라우저에서 `http://127.0.0.1:8765`를 연다. 현재 `.venv`가 존재하지 않는 Python 경로를 참조한다면 삭제 후 재생성하거나 설치된 Python으로 새 가상환경을 만들어야 한다.

### 10.3 단계별 재현

```powershell
# 운영 격자 비교와 테스트 예측 생성
python compare_operational_grid_sizes.py

# 최신 테스트 이벤트의 행정 문서 생성
python generate_agent_documents.py

# 데모용 JS 데이터 재생성(선행 산출물 필요)
python demo/build_demo_data.py

# 웹 데모 서버 실행
python demo/server.py --port 8765
```

전체 민감도 실험과 기상 수집은 시간이 더 걸리거나 API 키가 필요할 수 있다.

```powershell
python sensitivity_early_prediction.py
python fetch_kma_weather.py
```

### 10.4 환경변수

`.env.example`을 `.env`로 복사한 뒤 실제 키를 넣는다. 실제 키가 든 `.env`는 저장소에 커밋하지 않는다.

Gemini 예시:

```dotenv
LLM_PROVIDER=gemini
GEMINI_API_KEY=실제_키
GEMINI_MODEL=gemini-3.6-flash
```

OpenAI 예시:

```dotenv
LLM_PROVIDER=openai
OPENAI_API_KEY=실제_키
OPENAI_MODEL=사용_가능한_모델_ID
```

외부 호출 없는 모드:

```dotenv
LLM_PROVIDER=template
```

기상 수집에는 `fetch_kma_weather.py`가 읽을 수 있는 기상청 API 키 설정이 별도로 필요하다.

## 11. 테스트

현재 자동화 테스트는 행정 Agent의 핵심 계약을 확인한다.

```powershell
python -m unittest discover -s tests -v
```

검증 항목은 문서 3종 생성, 사람 검토 플래그, 원인 시설 비확정 문구, 1km 외 격자 거부, 템플릿 모드의 API 비활성화, Gemini 구조화 응답 처리다. 모델 파이프라인 전체를 빠르게 검증하는 단위·통합 테스트는 아직 부족하므로 모델 코드를 수정할 때는 결과 JSON과 CSV를 재생성하고 지표 변화 및 시간 누수 여부를 함께 확인해야 한다.

## 12. 설계상 장점

- **운영 목표가 명확하다.** 일반적인 정확도보다 “Top 3를 어디에 보낼 것인가”에 맞춰 학습·평가한다.
- **미래 검증을 사용한다.** 이벤트를 시간순으로 나눠 과거로 학습하고 미래만 테스트한다.
- **공간 해상도와 현장 비용을 함께 본다.** 넓은 격자의 높은 적중률만 좇지 않고 점검 면적을 비교한다.
- **예측과 문서 생성을 분리한다.** LLM이 모델 점수나 순위를 바꿀 수 없고 안전 템플릿으로 대체 가능하다.
- **한계를 결과에 포함한다.** 민원을 실제 악취·원인 시설·행정처분과 동일시하지 않는다.
- **오프라인 발표가 가능하다.** 외부 API 장애나 키 부재에도 템플릿 모드와 저장된 검증 결과로 시연할 수 있다.

## 13. 현재 한계와 개선 방향

### 현재 한계

- 테스트 이벤트가 48개로, 연도·계절·민원 제도 변화에 대한 추가 검증이 필요하다.
- 신고 데이터는 시민의 인지, 거주 분포, 신고 의향과 반복 신고의 영향을 받는다.
- 민원 위치는 실제 악취 발생지나 배출원 위치와 같지 않다.
- 장기간 센서 시계열, 출동·도착 시각, 현장 감지, 측정값, 조치 이력이 없다.
- 모델 점수는 확률 보정값이 아니라 이벤트 내부 순위 점수다.
- 기상·센서·시설 데이터를 추가했을 때의 일관된 성능 향상 근거가 아직 없다.
- 웹 서버는 로컬 시연용이며 인증, 권한, 동시 사용자, 운영 로깅을 갖춘 배포형 서버가 아니다.

### 권장 개선 순서

1. 연도별 롤링 백테스트와 부트스트랩 신뢰구간으로 성능 안정성을 확인한다.
2. 반복 신고자·인구 밀도·시간대 노출 편향을 분석하고 평가 지표를 보완한다.
3. 현장 출동과 측정 결과를 표준 스키마로 축적해 “민원 적중”과 “실제 악취 확인”을 분리 평가한다.
4. 충분한 기간의 센서·기상 자료가 쌓인 뒤 동일한 시간 분할로 추가 가치(ablation)를 검증한다.
5. 운영 전에는 격자 좌표계, 개인정보 비식별화, 접근 권한, 감사 로그, 장애 대응을 정식 설계한다.
6. RAG가 필요하다면 예측값 생성보다 대응 매뉴얼·법령·과거 점검 이력·문서 양식 검색에 제한해 사용한다.

## 14. 개발 시 주의사항

- `test_predictions.csv`의 `score`는 확률이라고 표시하지 않는다.
- 데이터 분할이나 `prior` 계산을 바꿀 때 테스트 미래 정보가 학습 특징에 들어가지 않는지 확인한다.
- 격자별 지표는 면적이 달라 단순 수치만으로 우열을 결론 내리지 않는다.
- 행정 Agent의 `ForecastResult` 계약(1km, Top 3, 순위 1~3)을 깨지 않는다.
- LLM 프롬프트를 수정해도 원인 시설 비확정, 새 사실 금지, 담당자 검토 조건은 유지한다.
- 웹 화면에 API 키를 넣거나 반환하지 않는다.
- 산출물 재생성 시 모델 지표, 데모 데이터, 문서 예시가 서로 같은 실험 결과를 가리키는지 확인한다.

---

이 문서는 현재 저장소의 코드, 설정, 저장된 지표와 `PROJECT_CONTEXT.md`를 기준으로 작성되었다. 모델이나 데이터가 갱신되면 특히 3장 데이터 규모, 5장 검증 결과, 10장 실행 방법을 함께 갱신해야 한다.
