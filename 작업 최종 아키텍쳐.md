# 작업 최종 아키텍처 (2026-09-20 기준)

익산시 악취 민원 예측 시스템의 최종 구조를 정리한 문서다. 수치는 모두 `outputs/*/metrics.json`에서 확인한 값이며, 현재 상태·한계는 [PROJECT_CONTEXT.md](PROJECT_CONTEXT.md), 실행 방법은 [README.md](README.md)가 기준이다.

## 한 장 요약

```
입력 데이터                     예측 엔진                                              출력 · 서비스
─────────────                 ─────────────────────────────────────────────          ─────────────
           ┌─▶ 과거 빈도·CPF ─┐
기상 ASOS ─┼─▶ 바람·강수·기온 ├▶ [1단] 발생 위험 예보  XGBoost R5, 매시 정각 ──┬──▶ 사전 경보 (참고): 상위 5 격자
기상 AWS ──┘   격자별 IDW    │    "민원 없는 지금, 다음 1시간 어디서 나나"       │
축산 시설 ─▶ 상풍측 6 km 노출┘    186개 격자 위험 순위                            │ 상위 30 격자 = 후보 축소 규칙
                                                                                 ▼
민원 원본 ────────────────────▶ [2단] 확산 예측  xgb_d3 (모델 변경 없음)  ──────▶ 이후 30분 1 km 권역 Top 3 (Hit@3 0.851)
                                  "민원 30분 쌓였다, 어디로 번지나"                │
                                  후보 31개 → 1단 상위 30 안 15개 → Top 3         ▼
                                                                         행정 대응 Agent (rule-base)
                                                                         문서 4종 + Streamlit 화면
                                                                         + 발생원 후보(Track A, 참고)
```

2단이 먼저 Top 3를 고르고, 이 세 격자는 후보 축소 과정에서도 항상 보존한다. 1단은 나머지 후보만 줄인다(테스트에서 후보 31 → 15개, Hit@1/2/3 변화 0). 1단 산출물이 없으면 후보 31개 전체를 사용한다. 문서에서 1단 상위 격자가 2단 Top 3와 겹치면 "(확산 예측 Top 3와 일치)" 표시가 붙는다. 기존 Top 3를 명시적으로 보호하므로 새 데이터에서도 후보 필터가 기존 예측을 제거하지 않는다.

## 1. 입력 데이터 계층

| 데이터 | 출처 | 어디에 쓰나 | 비고 |
|---|---|---|---|
| 악취 민원 14,994건 (2019-05~2026-08) | 익산시 (엑셀) | 1단·2단 라벨, 과거 빈도, CPF | 고유 좌표 11,944개 → 주소 단위 |
| ASOS 시간자료 (전주 146·군산 140) | 기상청 API허브 `kma_sfctm3` | 1단 기상 특징 | 실시간 호출 확인(정시 후 ≤6분) |
| AWS 시간·분자료 (익산 702·함라 733·여산 763·김제 737·진봉 736) | 기상자료개방포털 파일셋 | 1단 바람(격자별 보간) | 실시간 분자료 API는 불안정 → 결측 시 ASOS만으로 자동 강등 |
| 축산농가 현황 (익산·김제) + EMEP/EEA NH3 배출계수 | 공공데이터 + 유럽 배출 인벤토리 | 1단 발생원 노출 | 축종별 사육두수 × 배출계수 |
| 건물·산업단지·공장·하수처리 등 외부 시설 | Codex 수집(3,127행) | 검토 결과 기여 없음 → 미채택 | 발표에서 "검토했으나 무효" |

## 2. 2단 확산 예측 (기존 모델, 손대지 않음)

- 질문: 민원이 30분 쌓였을 때, 이후 30분 안에 추가 민원이 날 1 km 격자는 어디인가
- Event 정의: 1시간에 민원 10건·5격자(500 m) 이상인 시각. 공통 Event 160개, 시간순 70/30 분할 → 테스트 48개(평가 가능 47개)
- 후보: 첫 30분 민원 격자 반경 3칸 (평균 31개)
- 입력 변수 15개: 첫 30분 자기 격자 민원 수·강도, 최근접 관측 격자 거리, 1.5칸·3칸 안 민원 수, 학습 Event 활성 비율(`prior`), 시각 sin/cos, 무게중심 거리, 최근접 강도, 강도 가중 평균, 관측 격자 수, 관측 건수, 첫 15분 건수, 증가량
- 모델: XGBoost `xgb_d3`(max_depth 3). 내부 검증으로 5개 후보 중 선택
- 성능(`outputs/operational_grid_comparison/metrics.json`): Hit@3 0.851, Hit@1 0.553, Hit@2 0.745. 1.5 km 격자는 Hit@3 0.957, 2 km 0.894
- 기상·발생원을 넣지 않는 이유: 소거 실험(`outputs/ablation/ablation_table.md`)에서 개선 0. 이후 30분 민원의 51%가 같은 격자, 89%가 2 km 안이라 위치가 이미 답이다

## 3. 1단 발생 위험 예보 (이번에 추가한 모델)

- 질문: 민원이 아직 없는 시각 t에, [t, t+1h) 안에 민원이 날 1 km 격자는 어디인가
- 학습 단위: (시각, 격자). 격자 186개. 학습 2020~2024(민원 없는 행은 3% 표본), 테스트 2025-01~2026-07
- 입력 변수 28개 (채택 실험군 R5):

| 묶음 | 변수 | 통용 개념 |
|---|---|---|
| 시간·관성 (10) | 시각·월 sin/cos, 주말, 야간, 격자 365일·30일 민원율, 최근 24h 민원 유무, 시 전체 30일 민원율 | 기저율 |
| 기상 (8) | 풍속, 풍향 sin/cos, 강수 1h·3h, 기온, 습도, 정체(풍속 <1 m/s) | ASOS+AWS 격자별 역거리 가중(IDW) |
| 발생원 노출 (6) | 상풍측 ±30° 6 km 축산 배출 합, 전방위 합, 비율 (배출계수·시설 수 2벌) | 수용체 모델(receptor model)의 상풍측 노출 |
| 풍향 조건부 민원 지도 (4) | 격자×풍향 30° 구간별 과거 1년 민원율 (전체·비율·가축·공장) | 조건부 확률 함수(CPF) |

- 역추적 구현 방식: 궤적 모델(HYSPLIT) 대신 수용체 모델. "지금 바람이 불어오는 쪽 6 km 안에 축산 배출이 얼마나 있나"와 "이 격자에서 이 풍향일 때 과거에 민원이 얼마나 났나"를 숫자로 만든다. 모든 변수는 t 이전 정보만 쓴다(누수 없음)
- 성능(`outputs/onset_risk/metrics.json`, 조용한 시각 Hit@5 = 직전 3시간 시 전체 민원 없던 시각에 186개 중 5개 찍기):

| 구성 | Hit@5 |
|---|---:|
| R0 시간 + 과거 빈도 | 0.546 |
| R1 + 기상 | 0.548 |
| R2 + 축산 발생원 노출 | 0.590 |
| R5 + 풍향 조건부 민원 지도 (채택) | 0.599 |
| 냄새 유형별 R5 | 가축 0.588 · 공장 0.747 · 하수 0.685 |

- 확정된 부속 결론(근거: `outputs/onset_risk/experiment_summary.md`, `outputs/wind_lag_sweep/`): 바람은 정시 값이 최선(시간 자료 24조합), 분 자료 60분 평균을 넣어도 모델 차이 없음. AWS 추가가 유일하게 효과(R5 0.581 → 0.599). 외부 시설 데이터·산업단지 경계·대기안정도·건물 밀도는 기여 없음

## 4. 두 모델의 결합 방식 (검증된 것만)

| 방식 | 결과 | 상태 |
|---|---|---|
| 문서에서 나란히 표시, 겹치면 표기 | 성능 영향 0 | 채택 |
| 2단 Top 3 중 1단 순위로 최종 1개 선택 (`fuse_onset_spread.py`) | Hit@1 0.553 → 0.553 (고침 5·망침 5) | 검증 후 무효 |
| 기존 2단 Top 3를 보호하고 나머지를 1단 위험 순위 30위 이내로 축소 (`administrative_agent/policy.py` `narrow_candidates`, 검증은 `fuse_onset_spread.py --narrow 30`) | 후보 31 → 15개, Hit@1/2/3 0.553/0.745/0.851 변화 0(고침·망침 0) | **채택.** 문서 생성·화면에서 기본으로 켜짐. 성능 향상이 아니라 후보 수 축소. 기존 Top 3는 순위 밖이어도 보존 |


왜 후보를 좁혀도 성능이 안 바뀌나: 2단 정답(이후 30분 추가 민원)은 89%가 첫 민원 2 km 안, 51%가 같은 격자라 후보 중심부에 몰려 있고, 2단 점수도 중심부가 높다. Event 1시간 전 1단 순위는 최근 24시간 민원·격자 과거 빈도가 좌우하므로 중심부는 자연히 상위 30 안에 든다(정답 격자의 1단 순위 중앙값 6위, 30위 이내 98.7%). 잘려 나가는 16개는 후보 가장자리의 민원 이력이 적은 칸이라 원래 Top 3에 오르지 않던 격자다. 실익은 정확도가 아니라 담당자가 보는 후보 수다.

## 5. 행정 대응 Agent (`administrative_agent/`)

- 역할: 예측 결과를 담당자가 쓰는 문서로 바꾼다. rule-base(상대위험 → 대응 단계, `administrative_agent/policy.py`)
- 입력(`administrative_agent/models.py` `ForecastResult`): 2단 Top 3(`RiskArea`), Track A 발생원 후보(`SourceCandidate`), 1단 사전 경보(`OnsetAlertCell`, 전체 + 유형별), 후보 축소 정보(후보 수 → 축소 후 수)
- 후보 축소(`generate_agent_documents.py`): 과거 Event는 Event 이전 자료로 학습한 가장 최신 `onset_event_scores*.csv`를 검증과 동일한 함수로 선택한다. 해당 점수가 없으면 `onset_alerts.csv` 상위 30과 `onset_cells.csv`를 사용한다. 기존 2단 Top 3를 보호한 뒤 나머지를 `narrow_candidates`로 줄인다.
- 출력(`outputs/administrative_agent/`): 민원 브리핑, 현장 점검 지시, 대응 가이드, 사후 보고 양식 4종 + `agent_output.json`
- 문구 규칙(테스트가 강제): 확률 아님·상대값, 원인 시설 미단정, 사전 경보는 "참고"
- 화면: `streamlit_app.py`에 Top 3 + 사전 경보 카드 + 유형별 캡션

## 6. 실행 흐름 (지금은 사후 계산, 실시간은 미구현)

```
python compare_operational_grid_sizes.py   # 2단 (기존)
python run_onset_risk.py                   # 1단 전체 → onset_alerts.csv
python run_onset_risk.py --label-type factory --tag factory --arms R0,R2,R5   # 유형별(livestock, sewage도 같은 방식)
python generate_agent_documents.py         # 문서 4종
streamlit run streamlit_app.py             # 화면
```

실시간으로 가려면 매시 :06 기상 호출 → 특징 계산 → 1단 추론 경로(미구현)와 민원 실시간 피드(기관 연계)가 필요하다.

## 7. 멘토 피드백 대응표

| 피드백 | 아키텍처에서의 답 |
|---|---|
| 2. 기상 왜 제외 / 축산 위치·과거 대기 경로 | 1단에 기상 8개 + 상풍측 노출 + CPF. 2단은 넣어도 개선 없음을 소거 실험으로 제시 |
| 2-2. 거리별 풍향 시차 조정 | 시간 24조합·분 19창 검정 → 정시가 최선. 거리÷풍속으로 시차를 두는 τ=d/u 방식(`test_wind_source_association.py --travel-lag`)도 정시보다 낮음(비 1.047 vs 1.065). 바람 이동 15분 + 신고 지연 29분이 정시 안에 들어옴 |
| 5. 역궤적·확산 모델 | 수용체 모델로 구현. HYSPLIT는 3 km 격자 기상장이라 6 km 문제에 해상도 부족 |
| 6. 상습 지역 ↔ 빈도, 라벨 특정화 | 격자 과거율·CPF가 그것. 유형별 모델(공장 0.747)이 라벨 특정화 |
| 7. 마을·동 단위 | 미착수. 설계만 완료(동·리 단위 재학습, 기준선 3개), 다음 과제 |
| 3·8. 적용가능성·output 명확화 | Agent 문서 4종 + 대응 단계. 실시간 조건(기상 가능, 민원 피드 필요) 명시 |

## 8. 한계 (발표에서 먼저 말할 것)

- 1단 절대 수준 60%는 과거 빈도만으로도 55%가 나오는 문제 위 +5%p. "잘 맞히던 걸 조금 더 맞힌다"
- 테스트 Event 47개라 Hit@3 신뢰구간 약 ±10%p
- 민원은 신고 행동이지 악취 그 자체가 아니다. 발생원 후보는 참고 정보
- 실시간 경로 미구현, 민원 피드 없음

## 9. 파일 지도

| 구분 | 파일 |
|---|---|
| 2단 | `build_odor_ai_mvp.py`, `sensitivity_early_prediction.py`, `optimize_early_prediction.py`, `compare_operational_grid_sizes.py`, `run_ablation.py` |
| 1단 | `run_onset_risk.py`(R0·R1·R2·R5·R5o만 남김), `wind_sources.py`, `species_weight_sets.py`, `test_wind_source_association.py`, `fetch_kma_weather.py` |
| 결합 검증 | `fuse_onset_spread.py` |
| 바람·발생원 검정 | `wind_window_sweep.py`, `compare_wind_sources.py`, `minute_wind_test.py`, `test_factory_source_filters.py`, `sensitivity_species_weights.py` |
| Track A 발생원 역추적 | `build_source_backtrack.py`, `extend_source_catalog.py`, `analyze_source_type_association.py`, `tools/` |
| 서비스 | `administrative_agent/`, `generate_agent_documents.py`, `streamlit_app.py`, `demo/` |
| 검증 | `tests/` |
